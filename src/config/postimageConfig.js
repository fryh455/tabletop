// PostImage API (opcional).
// Se não quiser usar, deixe enabled=false e o uploader vai aceitar URL manual.
export const postimageConfig = {
  enabled: false,
  // Em algumas integrações você pode precisar de uma key/token.
  apiKey: "REPLACE_ME",
  endpoint: "https://api.postimages.org/1/upload"
};
